# Coleta atual usa apenas httpx (registries JSON/APIs) + git clone via subprocess.
httpx>=0.27.0

# Opcionais — só necessárias se você adicionar um adapter que faça parsing de HTML
# ou navegação com navegador. Descomente conforme a necessidade:
# beautifulsoup4>=4.12.0   # parser HTML offline
# playwright>=1.44.0       # navegação/render de páginas client-side
